# Nexus Annotation Platform Frontend

This folder contains the React + TypeScript + Vite frontend for the annotation platform.

## Commands

Run these from `frontend/`:

```bash
npm install
npm run dev
npm run build
npm run lint
```

## Notes

- Firebase client configuration lives in `src/firebase.ts`
- Environment variables for the frontend live in `frontend/.env`
