# Backend

This folder is reserved for backend code for the Nexus Annotation Platform.

## Current state

- The existing project logic still uses Firebase directly from the frontend.
- No standalone API or server implementation existed in the original repository.

## Next steps

- Add your server entry point in `src/`
- Define backend dependencies in `backend/package.json`
- Replace the scaffold script with your real dev/build workflow
